import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'daily-trivia-challenge-v2',
  webDir: 'www'
};

export default config;
